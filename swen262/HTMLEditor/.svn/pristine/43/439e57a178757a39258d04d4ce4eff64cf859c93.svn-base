package validator;

/**
 * This interface is responsible for declaring the one
 * needed method that any concrete container class must implement;
 * 		getIterator() - gets the Class's iterator
 * 
 */
public interface Container {
	public Iterator getIterator();
}
